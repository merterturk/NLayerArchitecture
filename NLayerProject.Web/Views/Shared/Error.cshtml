@model ErrorDto
@{
    ViewData["Title"] = "Error";
}

<div class="alert-danger">Bir Hata Meydana geldi</div>
<div class="alert-danger mt-2">
    @foreach (var item in Model.Errors)
    {
        <p>@item</p>
    }
</div>
<div class="mt-3 alert-success">
    Hata mesajları için asdfasdfas@hotmail.com mesaj atabilirsiniz
</div>